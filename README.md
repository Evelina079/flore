# 🌸 Flore --- Онлайн-магазин цветов

**Flore** --- учебный backend-проект для демонстрации работы с
**FastAPI**, **PostgreSQL** и **Docker**.\
Проект реализует базовые **CRUD API** для:

-   👤 пользователей\
-   💐 букетов\
-   🧾 заказов

------------------------------------------------------------------------

## 🚀 Возможности

-   Аутентификация пользователей
-   Управление букетами
-   Оформление заказов
-   Документация API через Swagger
-   Работа в Docker-контейнерах
-   Подключение mock-сервиса
-   Тестирование через pytest

------------------------------------------------------------------------

## 🗂 Структура проекта

``` text
flore/
├─ backend/
│ ├─ app/
│ │ ├─ api/v1/
│ │ │ ├─ auth.py
│ │ │ ├─ bouquets.py
│ │ │ └─ orders.py
│ │ ├─ crud.py
│ │ ├─ db/
│ │ │ ├─ base.py
│ │ │ └─ session.py
│ │ ├─ main.py
│ │ ├─ models.py
│ │ ├─ schemas.py
│ │ └─ core/
│ │    └─ security.py
│ ├─ mock_service.py
│ └─ requirements.txt
├─ docker-compose.yml
├─ Dockerfile
├─ seed.sql
└─ README.md
```

------------------------------------------------------------------------

## 🛠 Используемые технологии

-   **Backend:** FastAPI (Python)
-   **База данных:** PostgreSQL
-   **ORM и миграции:** SQLAlchemy + автоматическое создание таблиц
-   **Контейнеризация:** Docker, docker-compose
-   **Mock-сервисы:** FastAPI mock-service
-   **Тесты:** pytest
-   **Документация:** OpenAPI / Swagger

------------------------------------------------------------------------

## ⚙️ Установка и запуск

### 1️⃣ Клонирование репозитория

``` bash
git clone https://github.com/Evelina079/flore.git
cd flore
```

------------------------------------------------------------------------

### 2️⃣ Запуск через Docker Compose

``` bash
docker-compose up --build
```

Будут запущены контейнеры:

  Сервис         Назначение                                Порт
  -------------- ----------------------------------------- ------
  backend        FastAPI API                               8000
  postgres       База данных                               5432
  mock-service   Внешний mock-сервис (если используется)   9000

------------------------------------------------------------------------

### 3️⃣ Загрузка seed-данных (опционально)

``` bash
docker exec -it flore-db-1 psql -U flore -d flore_db -f /seed.sql
```

------------------------------------------------------------------------

### 4️⃣ Доступ к приложению

-   🚀 API:\
    `http://localhost:8000`

-   📘 Swagger / OpenAPI:\
    `http://localhost:8000/docs`

------------------------------------------------------------------------

## 🔗 Примеры запросов (cURL)

### ✅ Просмотр всех букетов

``` bash
curl -X GET "http://localhost:8000/api/v1/bouquets/"
```

------------------------------------------------------------------------

### ✅ Создание заказа

``` bash
curl -X POST "http://localhost:8000/api/v1/orders/" -H "Content-Type: application/json" -d '{"user_id":1,"total_price":1500,"delivery_address":"ул. Ленина, 10"}'
```

------------------------------------------------------------------------

## 🧪 Тестирование

### 1️⃣ Установка зависимостей

``` bash
pip install -r backend/requirements.txt
pip install pytest
```

------------------------------------------------------------------------

### 2️⃣ Запуск тестов

``` bash
pytest
```

------------------------------------------------------------------------

## 📖 Документация API

Документация автоматически генерируется FastAPI и доступна по адресу:

``` text
http://localhost:8000/docs
```

В Swagger можно: - посмотреть все эндпоинты, - изучить модели данных, -
протестировать запросы прямо из браузера.

------------------------------------------------------------------------

## ✅ Статус проекта

📌 Учебный проект\
📌 Готов для использования в портфолио\
📌 Отлично подходит для демонстрации backend-навыков
